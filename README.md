# Semantic Prompt Architecture (SPA)

Semantic Prompt Architecture (SPA) is a common architecture for representing human intent as Semantic State and translating it into model-specific prompts.

The goal is:

Human → Semantic State → Prompt → AI

rather than

Human → Prompt → AI.
